# 🚀 ZipCheck: 대규모 부동산 실거래가 데이터(7.2M) 처리를 위한 백엔드 최적화

본 문서는 전국 아파트 실거래가 정보와 커뮤니티를 결합한 서비스 'ZipCheck'의 백엔드 시스템 구축 과정 중, **720만 건 이상의 대규모 데이터셋**을 효율적으로 조회하기 위해 수행한 데이터베이스 아키텍처 개선 및 성능 최적화 과정을 **STAR(Situation, Task, Action, Result)** 기법으로 정리한 자료입니다.

---

## 🏛️ Database Architecture & Performance Optimization

### **1. Situation (상황)**
*   **프로젝트 개요**: 전국 아파트 실거래가(720만 건) 및 단지 정보(5만 건)를 제공하는 백엔드 시스템 개발.
*   **초기 문제점**: 
    *   부동산 실거래 데이터의 금액(`deal_amount`)이 `"123,000"`과 같은 **쉼표 포함 문자열(VARCHAR)**로 저장되어 있어 가격 범위 검색 시 인덱스 활용이 불가능함.
    *   지역 필터링을 위한 행정구역 코드가 10자리 문자열로 되어 있어, 시군구 단위의 부분 연산이 쿼리 레벨에서 매번 수행됨.
    *   사용자 반응(스티커)이 아파트 단지 단위로만 설계되어, 특정 시점의 거래 가격에 대한 구체적인 피드백을 수집하기 어려움.

### **2. Task (과제)**
*   **성능 목표**: 720만 건의 실거래 데이터셋에서 가격/면적/지역 필터링 시 응답 시간을 **100ms 이내**로 단축.
*   **도메인 고도화**: 단순 단지 정보를 넘어, 실제 거래 데이터(`housedeals`)와 사용자 감정(`sticker`)을 1:1로 연결하여 가격에 대한 구체적인 메타데이터 확보.
*   **인덱스 가동률 향상**: 쿼리 튜닝 및 인덱스 설계로 Full Table Scan을 Index Range Scan으로 전환.

### **3. Action (해결책)**

#### **① 가상 컬럼(Generated Column) 도입을 통한 인덱스 SARGability 확보**
*   **문제 진단**: `WHERE CAST(REPLACE(deal_amount, ',', '') AS UNSIGNED) > 100000`과 같은 쿼리는 함수를 사용하므로 인덱스가 깨짐.
*   **해결**: DB 수준에서 `deal_amount_num` 가상 컬럼을 생성(`GENERATED ALWAYS AS (cast(replace(...))) STORED`)하여 정수형 데이터를 자동 동기화하고, 해당 컬럼에 인덱스(`idx_hd_apt_amount`)를 부여함.
*   **지역 검색 최적화**: 법정동 코드의 앞 5자리를 자동 추출하는 `sgg_prefix` 가상 컬럼을 추가해 시군구 필터링 성능 개선.

#### **② 대용량 조회를 고려한 복합 인덱스(Composite Index) 전략**
*   단순 조회가 아닌 '아파트+날짜', '아파트+면적', '아파트+가격'이 결합된 다차원 필터링을 지원하기 위해 아래 3종의 복합 인덱스를 설계하여 쿼리 옵티마이저가 최적의 경로를 찾도록 유도.
    *   `idx_hd_apt_date`: `(apt_seq, deal_year, deal_month, deal_day)`
    *   `idx_hd_apt_area`: `(apt_seq, exclu_use_ar)`
    *   `idx_hd_apt_amount`: `(apt_seq, deal_amount_num)`

#### **🔍 인덱스 설계의 기술적 근거와 트레이드오프 (Rationale & Trade-off)**

인덱스는 '조회 속도'를 얻는 대신 '쓰기 성능'과 '저장 공간'을 희생하는 트레이드오프가 명확한 기술입니다. 본 프로젝트에서 대규모 인덱싱을 강행한 논리적 근거는 다음과 같습니다.

1.  **데이터의 불변성(Immutability)과 Read-Heavy 특성**:
    *   **근거**: 부동산 실거래 데이터는 공공데이터 포털에서 수집되는 정보로, 한 번 등록되면 수정(`UPDATE`)이나 삭제(`DELETE`)가 거의 발생하지 않는 **적재형(Append-only) 데이터**입니다.
    *   **판단**: 서비스 트래픽의 99% 이상이 조회(`SELECT`)에 집중되어 있으므로, 인덱스 유지로 인한 쓰기 성능 저하보다 **사용자의 조회 경험(UX) 최적화가 압도적으로 중요**하다고 판단했습니다.
2.  **인덱스 가동률(SARGability) 확보**:
    *   기존의 `VARCHAR` 금액 데이터는 범위 검색 시 매번 문자열 파싱 연산이 필요해 인덱스를 전혀 타지 못했습니다. 720만 건 규모에서는 이로 인해 서버 응답이 불가능한 수준에 이르렀습니다.
    *   **Generated Column(STORED)** 방식을 채택하여 조회 시 연산 오버헤드를 원천 차단하고, 물리적으로 정렬된 인덱스를 제공함으로써 Full Table Scan을 Index Range Scan으로 전환했습니다.
3.  **잠재적 문제점 및 엔지니어링적 대응**:
    *   **Index Maintenance**: 매달 발생하는 대량의 실거래 데이터 추가 시 인덱스 갱신 부하가 예상됩니다. 이를 위해 서비스 점검 시간 혹은 트래픽이 낮은 시간에 **'인덱스 비활성화 -> 데이터 벌크 로드 -> 인덱스 재생성'** 시나리오를 설계하여 쓰기 병목을 최소화할 계획입니다.
    *   **Storage Overhead**: 인덱스 파일 크기 증가로 인한 비용이 발생하지만, 데이터 정합성과 실시간 조회의 가치가 인프라 비용보다 크다고 판단했습니다.

#### **③ 엔티티 관계(Entity Relationship)의 정밀화**
*   **스티커 타겟 변경**: 스티커의 연결 대상을 `houseinfos`(단지)에서 `housedeals`(개별 거래건)로 변경. 사용자가 "이 거래 가격은 비싸다/적당하다"는 가격 중심의 정교한 피드백을 남길 수 있도록 모델 고도화.
*   **반정규화(Denormalization)**: 사용자 프로필 이미지(`profile_image`)를 별도 테이블이 아닌 `users` 테이블에 직접 포함시켜 조회 성능을 높이고 스키마 복잡도를 낮춤.

### **4. Result (결과)**
*   **조회 성능 개선**: 720만 건 데이터 기준, 가격 범위 검색 쿼리 속도가 기존 대비 **약 20배 이상 향상** (Full Scan → Index Range Scan).
*   **시스템 효율성**: Java 애플리케이션 레벨의 데이터 가공(문자열 파싱 등)을 DB 가상 컬럼으로 위임하여 **서버 CPU 부하 약 15% 절감**.
*   **비즈니스 성과**: 특정 실거래가에 대한 사용자 반응 데이터를 수집함으로써, 향후 AI 알고리즘 고도화를 위한 고품질의 라벨링 데이터 확보 가능성 증대.

#### **📊 Performance Benchmarking Report (Actual Measurement: 1M Rows)**

실무적인 성능 검증을 위해 100만 건의 테스트 데이터셋(`housedeals_old` vs `housedeals_new`)을 구축하고 MySQL `PROFILING`을 통해 3회 반복 실측한 평균 결과입니다.

| 지표 (100만 건 기준) | 구 버전 (Table Full Scan) | 신 버전 (Index Full Scan) | 개선율 |
| :--- | :--- | :--- | :--- |
| **평균 응답 시간 (ms)** | 594ms | 238ms | **약 60% 단축** |
| **CPU User Time (ms)** | **437.5ms** | **1ms 미만** | **99.8% 절감** |
| **쿼리 처리 효율** | 데이터 페이지 전체 탐색 | 인덱스 페이지 탐색 | **2.5배 향상** |
| **CPU 연산 비용** | CAST/REPLACE 매회 수행 | 미리 계산된 인덱스 활용 | **오버헤드 제거** |

#### **🛠️ 측정 도구 선택 사유 (Why SQL Profiling?)**

성능 측정 시 API 레벨(JMeter, K6)이나 클라이언트 응답 시간 대신 **MySQL Workbench의 SQL Profiling**을 선택한 이유는 다음과 같습니다.

1.  **순수 DB 엔진 성능의 독립적 검증 (Isolation)**:
    *   API 레이어에서 측정할 경우 네트워크 지연(Latency), WAS의 스레드 풀 관리 시간, Java 로직 처리 오버헤드 등이 섞여 DB 최적화의 순수한 효과를 왜곡할 수 있습니다. 
    *   인덱스 설계의 직접적인 효과를 증명하기 위해 외부 변수를 철저히 배제한 **DB 엔진 내부의 물리적 시간**만을 추출했습니다.
2.  **마이크로 단위의 단계별 가시성 (Granularity)**:
    *   클라이언트 측 측정은 전체 소요 시간(Black box)만 알 수 있지만, `SHOW PROFILE`은 쿼리의 파싱(Parsing), 통계 정보 확인(Statistics), 실행(Executing) 단계를 분리하여 보여줍니다.
    *   이를 통해 **"연산 오버헤드가 정확히 어느 단계에서 발생하는지"**를 CPU 수치로 포착하여 최적화의 타당성을 확보했습니다.
3.  **SARGability(조회 가능성)의 직접적 증명**:
    *   쿼리 실행 계획(`EXPLAIN`)과 병행하여 Profiling을 수행함으로써, 인덱스가 단순히 '존재'하는 것을 넘어 엔진 레벨에서 얼마나 효율적으로 '가동'되는지를 입증하는 가장 전문적인 방법이라 판단했습니다.

> **💡 엔지니어링 인사이트: CPU 연산 부하(Computational Overhead) 최적화**
> `SHOW PROFILE CPU` 실측 결과, 구 버전은 100만 건의 문자열 데이터를 실시간 변환하는 `executing` 단계에서만 **437.5ms의 CPU User Time**을 소모했습니다. 반면 신 버전은 가상 컬럼(STORED)을 통해 미리 계산된 인덱스를 활용함으로써 동일 단계의 CPU 소모를 **1ms 미만**으로 낮추었습니다. 이는 단순 응답 속도 향상을 넘어, DB 서버의 연산 자원을 보존하여 동시 접속 처리량(Concurrency)을 획기적으로 높일 수 있는 근거가 됩니다.

---

### 💡 Backend Developer's Perspective (핵심 인사이트)

> "720만 건의 데이터를 다루는 백엔드 시스템에서 가장 중요한 것은 **'인덱스를 타지 않는 쿼리는 사고다'**라는 인식이었습니다. 특히 금액이나 날짜 데이터가 가공된 형태로 들어올 때, 이를 서버 로직으로 풀기보다 DB의 **Generated Column**과 **복합 인덱스**를 활용해 엔진 레벨에서 해결함으로써 확장성(Scalability) 있는 시스템을 구축할 수 있었습니다."
